package pt.isec.amov.quizec.model

data class QuestionOption(
    //val id: String,
    val text: String,
    //TODO: single class OR extend QuestionOption (?)
    // val isCorrect: Boolean,
    // val matchingPair: String?
)
